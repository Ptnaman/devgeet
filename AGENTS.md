# DevGeet project instructions

- Work only inside `W:\App-dev\Expo_project\devgeet` for this project.
- The product name is `DevGeet`.
- This app uses Expo SDK 57 and Expo Router. Always use the relevant Expo skills before changing Expo app code.
- Check the installed package versions and existing architecture before implementation.
- Use the versioned Expo SDK 57 documentation when current framework behavior needs verification: https://docs.expo.dev/versions/v57.0.0/
- Preserve existing user changes and keep edits limited to the requested feature.
- Keep existing SVG assets unchanged unless the user explicitly requests an SVG change.
- Use `expo-image` with the existing blur placeholder and transition setup for remote images.
- Respect safe-area insets for headers, bottom tabs, sheets, and Android system navigation.
- Run TypeScript and relevant Expo validation after code changes, and report separately whether device testing or deployment was performed.